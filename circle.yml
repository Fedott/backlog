machine:
    php:
        version: 7.0.11
test:
    override:
          - mkdir -p $CIRCLE_TEST_REPORTS/phpunit
          - cd amphp && composer install
          - cd amphp && ./vendor/bin/phpunit --log-junit $CIRCLE_TEST_REPORTS/phpunit/junit.xml -c .

